{
  "name": "interactions-lab1",
  "version": "1.0.0",
  "description": "Action of Lab1 in Interactions section",
  "main": "index.js",
  "scripts": {
    "prepare": "NODE_OPTIONS=--openssl-legacy-provider ncc build index.js -o .github/actions/lab3/dist --source-map"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/glob": "^0.5.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.31.1"
  }
}